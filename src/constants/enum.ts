export enum HomeSiderType {
  HOME = 'home',
  POST = 'post', // has table of contents
  NONE = 'none',
}

export enum HomeSiderSegmentType {
  INFO = 'info',
  DIRECTORY = 'directory',
  SERIES = 'series',
}
